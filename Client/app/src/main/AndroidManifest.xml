<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.goreatudor.client">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.Client">
        <activity
            android:name=".activities.activities.MainActivity"
            android:exported="false" />
        <activity
            android:name=".activities.activities.SelectActivity"
            android:exported="false" />
        <activity
            android:name=".activities.activities.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activities.activities.LogInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>